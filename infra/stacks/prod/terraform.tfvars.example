# Copy to terraform.tfvars and set real values. terraform.tfvars is gitignored.

domain_aliases = ["flexigom.com", "www.flexigom.com"]

# Override only if needed:
# site_bucket_name = "flexigom-frontend-prod"
# deploy_branch    = "main"
